[project]
name = "oagent"
version = "0.1.0"
description = "An autonomous offensive security agent (research/educational)"
dependencies = ["groq>=0.11"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
